apiVersion: v1
kind: Service
metadata:
  name: postgres-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: postgres
  ports:
  #port y targetPort para que postgres se conecte a redis
    - port: 5432
      targetPort: 5432
